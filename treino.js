const pergunta = require("readline-sync");

/*
const num = pergunta.questionInt('Digite um número qualquer: ');

function dobro(num) {
    return num * 2
}

function metade(num) {
    return num / 2
}

console.log(`O dobro de ${num} é ${dobro(num)} e metade é ${metade(num)}`);

*/

const numero = pergunta.questionInt('Digite um número: ');

function raizQuadrada (numero) {
    
}

